# Development / test dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0  # used by FastAPI TestClient transport
